"use client";
// This file is kept for structure, but the primary hook logic is in AuthContext.
// For simplicity, we re-export from there if needed, or it can be removed if components import directly from context.
export { useAuth } from '@/contexts/auth-context';
